# Phase 6: Auth Unification & Multi-Tenancy

## Overview
Synthesis of Mobile Resident identity (`users/{uid}`) and SaaS Operator custom-claims into a Unified Identity Architecture, preventing "Profile not found" fragmentation and allowing Operators to manage multiple tenants seamlessly.

## Completed ✅
- [x] Expanded `AppUser` types for multi-tenant arrays.
- [x] JIT (Just-In-Time) Profile Provisioning added to `appUserService` (`findOrCreate`).
- [x] Operator profiles autogenerated transparently upon login.
- [x] Authentication Middleware refactored for unified profile checking.
- [x] Built TopNav Organization Switcher context.

## Remaining 📋
- [ ] Stabilize mobile test environments and perform end-to-end integration tests on Native App.
- [ ] Address non-fatal console warnings with Firebase tokens in Dev Server.
- [ ] Evaluate nested routing for super_admin tenant-jumping logic.

## Notes
- "Resident" logic was separated to purely depend on Firebase Auth UID and global identity document, enabling independent mobile app interactions.
