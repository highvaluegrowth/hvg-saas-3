# Same as .gitignore but allows Firebase config files through to EAS build servers.
# EAS reads .easignore first; if present, .gitignore is NOT used during upload.
node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.env.local
.env.*.local
.agent/
.claude/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli
expo-env.d.ts
# @end expo-cli

# NOTE: google-services.json and GoogleService-Info.plist are intentionally
# NOT listed here so EAS can receive them during the cloud build.
